# Google Cloud Console → APIs & Services → Credentials → Create Credentials
# → OAuth client ID → Application type: "Web application".
# Add your GitHub Pages URL (e.g. https://<user>.github.io/<repo>/) to
# "Authorized JavaScript origins". This ID is public by design (Google
# Identity Services' token-client flow needs no client secret) — it's safe
# to bake into the built bundle, unlike desktop's OAuth client secret.
VITE_GOOGLE_CLIENT_ID=

# console.anthropic.com → API Keys → Create Key. Give it its own spend
# limit sized for script generation only (Milestone 3) — this is a static
# site with no backend, so this key is technically public in the built
# bundle (same tradeoff as the Google Client ID above), mitigated by that
# limit rather than by hiding it.
VITE_ANTHROPIC_API_KEY=
